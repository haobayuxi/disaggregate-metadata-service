// #include "dmc.h"

// bool DMC::CheckUpdater(std::vector<CasRead>& pending_updater) {
//   for (auto& up : pending_updater) {
//     if (*((uint64_t*)up.cas_buf) != *((uint64_t*)up.cas_buf)) {
//     }
//   }
//   return true;
// }
